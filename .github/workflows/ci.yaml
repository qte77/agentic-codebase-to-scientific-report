name: CI

on:
  push:
    branches: [main]
  pull_request:

# Least-privilege default token scope; steps elevate only if needed.
permissions:
  contents: read

# Cancel superseded runs on the same ref to save runner time.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-and-validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          fetch-depth: 0 # full history so gitleaks can scan all commits

      - name: Scan for secrets (gitleaks)
        uses: gitleaks/gitleaks-action@e0c47f4f8be36e29cdc102c57e68cb5cbf0e8d1e # v3.0.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Lint shell scripts
        run: make lint_sh

      - name: Lint Markdown (recursive; org lint workflow only covers top-level)
        run: make lint_md

      - name: Validate analysis.yaml schema contract (TDD)
        run: make test

      - name: Validate pipeline Makefile (dry-run, no LLM cost)
        run: make -n all
