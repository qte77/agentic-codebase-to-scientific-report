{
  // markdownlint-cli2 runner options. Rule config lives in .markdownlint.jsonc
  // (shared verbatim by `make lint_md` and the org CI lint workflow).
  "globs": ["**/*.md"],
  "ignores": [
    "results/**",
    "node_modules/**",
    "scripts/writeup/README.md"
  ]
}
